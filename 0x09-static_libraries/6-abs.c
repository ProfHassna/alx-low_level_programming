#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @a: The character to check
 * Return: value (sucess)
 */

int _abs(int a)
{
	int i;

	i = abs(a);
	return (i);
}
