#include "main.h"
#include <unistd.h>
#include <stdio.h>
/**
 * add - A function that adds two integers and returns the result.
 * Return: result
 */
int add(int a, int b)
{
	int somme;

	somme = a + b;
	return (somme);
}
