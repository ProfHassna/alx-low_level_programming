#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts - a function that prints a string, followed by a new line, to stdout
 * @str: param to print
 * Return: nothings.
 */

void _puts(char *str)
{
	puts(str);
	/*_putchar('\n');*/
}
