#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen - a function that returns the length of a string
 * @s: param to update
 * Return: nothings.
 */

int _strlen(char *s)
{
	unsigned int size;

	size = strlen(s);
	return (size);
}
