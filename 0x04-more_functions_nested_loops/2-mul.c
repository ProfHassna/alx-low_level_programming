#include "main.h"
#include <stdio.h>

/**
 * mul - A function that multiplies 2 int
 * @a: The number to multiply
 * @b: The number to multiply
 * Return: the result
 */

int mul(int a, int b)
{
	int i;

	i = a * b;
	return (i);
}
